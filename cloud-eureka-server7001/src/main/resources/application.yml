server:  port: 7001
eureka:
 instance:
   hostname: eureka7001.com #eureka服务端的实例名称  相当于域名
 client:
   register-with-eureka: false #false表示不向注册中心注册自己。
   fetch-registry: false #false表示自己端就是注册中心，被别人搜索
   service-url:
     defaultZone: http://eureka7002.com:7002/eureka/   #配置注册中心的地址